name: Keep Streamlit Alive

on:
  schedule:
    - cron: "17 */10 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Visit Streamlit
        run: |
          python -c "
          import urllib.request
          urllib.request.urlopen('https://ragaianalytics-3kjywchqr2u4xgjbmxxdnn.streamlit.app/')
          print('Success')
          "
