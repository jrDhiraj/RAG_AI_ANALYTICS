name: Keep Streamlit Alive

on:
  schedule:
    - cron: '17 */8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: |
          curl -L https://ragaianalytics-3kjywchqr2u4xgjbmxxdnn.streamlit.app/
